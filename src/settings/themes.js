import { NODEDIMENSIONS, OUTLINE1 } from "./CONSTANTS"
    export const defaultTheme = {
        empty   : NODEDIMENSIONS + OUTLINE1 + 'bg-gray-100',
        visited : NODEDIMENSIONS + OUTLINE1 + 'bg-blue-400',
        wall    : NODEDIMENSIONS + OUTLINE1 + 'bg-gray-600',
        start   : NODEDIMENSIONS + OUTLINE1 + 'bg-green-500',
        finish  : NODEDIMENSIONS + OUTLINE1 + 'bg-red-500',
        path    : NODEDIMENSIONS + OUTLINE1 + 'bg-orange-500',
    }
